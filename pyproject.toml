[project]
name = "credit"
version = "0.1.0"
description = "Flask application for credit applications with SQLite storage and AJAX status updates"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
]

